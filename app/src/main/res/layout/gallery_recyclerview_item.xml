<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.cardview.widget.CardView
        android:id="@+id/project_card_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/cardview_padding"
        android:minWidth="@dimen/touch_target_min"
        android:minHeight="@dimen/touch_target_min">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/project_recyclerview_constraint_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorLightPrimary">

            <!-- Image for project -->
            <ImageView
                android:id="@+id/project_image"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:background="@color/darkImagePlaceHolder"
                android:contentDescription="@string/project_image_content_description"
                app:layout_constraintDimensionRatio="1:1"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0.5"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"/>

            <!-- Layout containing schedule information -->
            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/schedule_layout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="60dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <!-- Gradient for schedule type display -->
                <ImageView
                    android:id="@+id/gallery_gradient_top_down"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    android:src="@drawable/gradient_overlay_top_down"
                    />
                <FrameLayout
                    android:id="@+id/schedule_icon_container"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent">
                    <!-- clock showing if the project has been scheduled and if its due -->
                    <ImageView
                        android:id="@+id/schedule_icon_due"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:contentDescription="@string/schedule_indicator_content_description"
                        android:src="@drawable/ic_schedule_indicator_due_24dp"
                        android:visibility="invisible" />
                    <!-- clock showing if the project has been scheduled and if its pending -->
                    <ImageView
                        android:id="@+id/schedule_icon_pending"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:contentDescription="@string/schedule_indicator_content_description"
                        android:src="@drawable/ic_schedule_indicator_pending_24dp"
                        android:visibility="invisible" />
                </FrameLayout>
                <TextView
                    android:id="@+id/gallery_item_schedule_indicator_days"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/white"
                    app:layout_constraintStart_toEndOf="@id/schedule_icon_container"
                    app:layout_constraintTop_toTopOf="parent"
                    />

                <LinearLayout
                    android:id="@+id/days_until_due_layout"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintEnd_toEndOf="parent">
                    <TextView
                        android:id="@+id/days_until_due_text_view"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/white"
                        android:layout_marginStart="@dimen/small"
                        android:layout_marginBottom="@dimen/small"
                        android:layout_marginEnd="2dp"
                        />
                    <ImageView
                        android:layout_marginBottom="2dp"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:src="@drawable/ic_wb_sunny_white_24dp"/>
                </LinearLayout>
            </androidx.constraintlayout.widget.ConstraintLayout>

            <!-- Layout showing a check if a photo is taken today -->
            <FrameLayout
                android:id="@+id/gallery_check_layout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="60dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent">
                <!-- Bottom gradient -->
                <ImageView
                    android:id="@+id/gallery_bottom_gradient"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:src="@drawable/gradient_overlay_bottom_up"
                    />
                <!-- check mark to indicate if photo has been taken today -->
                <ImageView
                    android:id="@+id/schedule_indicator_check"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:contentDescription="@string/schedule_indicator_check_content_description"
                    android:src="@drawable/ic_check_schedule_green_40dp"
                    android:layout_gravity="bottom|start" />
            </FrameLayout>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>