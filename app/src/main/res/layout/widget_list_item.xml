<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/widget_grid_item_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/detail_small_margin"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/widget_list_item_image_view"
        android:contentDescription="@string/widget_grid_item_content_description"
        android:layout_width="@dimen/widget_size"
        android:layout_height="@dimen/widget_size"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/detail_margin"
        app:layout_constraintStart_toEndOf="@id/widget_list_item_image_view"
        app:layout_constraintTop_toTopOf="@id/widget_list_item_image_view">

        <TextView
            android:id="@+id/widget_list_item_name_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Project Name"/>

        <TextView
            android:id="@+id/widget_list_item_time_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            tools:text="11:45 AM" />

    </LinearLayout>

</LinearLayout>